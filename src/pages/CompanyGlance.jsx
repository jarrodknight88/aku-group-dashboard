import StubPage from '../components/StubPage.jsx'

export default function CompanyGlance() {
  return (
    <StubPage
      active="company"
      level="Level 1 — Company Glance"
      title="Company Glance"
      blurb="Org-wide roll-up: headline strip, Money In / Saved / Protected, top sellers, and the location comparison table. Not part of this build slice — use the nav to reach the Location Report."
    />
  )
}
