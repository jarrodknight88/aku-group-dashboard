# Netlify build config — site: aku-group-dashboard
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: all routes are handled client-side by React Router.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
